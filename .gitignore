# Claude / Agents hub — machine-specific junctions, not part of the project
.claude/

# OS / editor
Thumbs.db
.DS_Store
*.swp
.vscode/

# Logs / temp
*.log
tmp/
temp/

# Secrets — never commit (e.g. VSCE_PAT for Marketplace publishing)
.env
.env.*

# Node
node_modules/

# VSIX packages
*.vsix

# Bundled usage-core binaries — added at package time, not committed to git
bin/usage-core-*
